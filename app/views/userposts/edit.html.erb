<h1>Editing Userpost</h1>
<div class="wrapper">
<%= form_with(model: @userpost, local: true, url: update_post_path(id: @userpost.id), method: "post") do |form| %>
  <% if @userpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userpost.errors.count, "error") %> prohibited this userpost from being saved:</h2>

      <ul>
        <% @userpost.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="ff">
  <div class="form-group">
    <%= form.text_field :title, placeholder: "Title", class: "field" %>
  </div>

  <div class="form-group">
    <%= form.text_area :content,placeholder: "Content", class: "field" %>
  </div>

  <div class="actions">
    <%= form.submit "Edit Post", class: "btn btn-primary" %>
  </div>
<% end %>
<%= link_to 'Back', view_profile_path %>
</div></div>