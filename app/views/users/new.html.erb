<h1>New User</h1>
<div class="wrapper">
<%= form_with(model: @user, local: true, url: create_user_path, method: "post") do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul class= "errors">
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="ff">
  <div class="form-group">
    <%= form.text_field :username, placeholder: "Username", class: "field" %>
  </div>
  <div class="form-group">
    <%= form.password_field :password, placeholder: "Password", class: "field" %>
  </div>
  <div class="form-group">
    <%= form.password_field :password_confirmation, placeholder: "Confirm Password", class: "field" %>
  </div>

  <div class="form-group">
    <%= form.text_field :name, placeholder: "Name", class: "field" %>
  </div>

  <div class="form-group">
    <%= form.text_area :bio, placeholder: "Bio", class: "field" %>
  </div>

  <div class="actions">
    <%= form.submit "Create User", class: "btn btn-primary"%>
  </div>
<% end %>
<%= link_to 'Back', login_path, class: "pull-right" %>
</div></div>
